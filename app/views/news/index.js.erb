// write a function that targets the news_div
//  Find news div
//  use innerHTML inserting the news partial.

var content = `<%= render 'index', news_array: @news_array %>`
var dispalyNewsOnClick = (content) => {
  const newsDiv = document.querySelector("#news-container")
  newsDiv.innerHTML = content
};

dispalyNewsOnClick(content)
