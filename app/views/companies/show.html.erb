<%= render 'shared/sidebar' %>

<div class="content-container col-10">
  <div class="row">
    <div class="col-6">
      <h1><%= get_company_name(@company.ticker) %></h1>
      <h2><%= @company.ticker %></h2>
      <h2><%= quote_endpoint(@company.ticker)[:price] %></h2>
    </div>
  </div>
  <%= line_chart @price_data_array, min: @min_price[0]*0.9, width: "100%", height: "500px", points: false %>
</div>
